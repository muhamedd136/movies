import React from 'react';
import './Details.scss';
import { withRouter } from 'react-router-dom';

const Details = () => {
  return (
    <div>
      <h1>Details</h1>
    </div>
  );
};

export default withRouter(Details);
